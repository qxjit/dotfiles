#!/usr/bin/env sh

set -e

if [ ! -d "$HOME/dotfiles" ]; then
  echo "Expected dotfiles repo to be cloned at $HOME/dotfiles. Aborting"
  exit 1
fi

./setup-scripts/link_configs
./setup-scripts/homebrews
./setup-scripts/vimstone
./setup-scripts/gcloud_sdk
